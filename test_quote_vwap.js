const http = require('http');
const https = require('https');

// Since we need the kite instance, let's just make a very rough script to use the credentials from .env
console.log("We need to check standard kite quote payload.");
